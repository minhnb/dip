'use strict';

describe('dipApp.report_event_reservations module', function() {

    beforeEach(module('dipApp.report_event_reservations'));

    describe('report_event_reservations controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var view1Ctrl = $controller('ReportEventReservationsController');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});