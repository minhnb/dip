'use strict';

describe('dipApp.profile module', function() {

  beforeEach(module('dipApp.profile'));

  describe('profile controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ProfileController');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});