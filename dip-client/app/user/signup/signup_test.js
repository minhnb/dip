'use strict';

describe('dipApp.signup module', function() {

  beforeEach(module('dipApp.signup'));

  describe('signup controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('SignUpController');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});