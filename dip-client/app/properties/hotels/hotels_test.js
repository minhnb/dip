'use strict';

describe('dipApp.properties_hotels module', function() {

    beforeEach(module('dipApp.properties_hotels'));

    describe('hotel controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var view1Ctrl = $controller('HotelController');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});