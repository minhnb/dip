'use strict';

describe('dipApp.properties_hotel module', function() {

    beforeEach(module('dipApp.properties_hotel'));

    describe('Hotel Profile Controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var view1Ctrl = $controller('HotelProfileController');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});