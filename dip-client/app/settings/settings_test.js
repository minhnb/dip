'use strict';

describe('dipApp.settings module', function() {

    beforeEach(module('dipApp.settings'));

    describe('settings controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var view1Ctrl = $controller('SettingsController');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});