'use strict';

describe('dipApp.event_reservations module', function() {

    beforeEach(module('dipApp.report_event_reservations'));

    describe('event_reservations controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var view1Ctrl = $controller('ReportEventReservationsController');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});